@extends('layouts.app')
@section('content')<main><section class="hero"><div class="wrap">@if($featured)<div class="grid"><article class="card">@if($featured->featured_image)<img class="media" src="{{ asset('storage/'.$featured->featured_image) }}" alt="{{ $featured->title }}">@else<div class="media"></div>@endif<div class="pad"><div class="kicker">Featured Story</div><h2><a href="{{ route('articles.show',$featured->slug) }}" style="color:inherit;text-decoration:none">{{ $featured->title }}</a></h2><p class="deck">{{ $featured->excerpt }}</p></div></article><aside>@foreach($latest->where('id','!=',$featured->id)->take(4) as $a)<div class="story"><div class="kicker">{{ $a->category?->name ?? 'Story' }}</div><h3><a href="{{ route('articles.show',$a->slug) }}" style="color:inherit;text-decoration:none">{{ $a->title }}</a></h3></div>@endforeach</aside></div>@else<div class="card"><div class="pad"><h2>Your publisher CMS is ready.</h2><p>Sign in at <strong>/admin</strong> and publish your first story.</p></div></div>@endif</div></section></main>@endsection

@extends('admin.layout') @section('heading','Articles') @section('content')<div class="card"><div class="row" style="justify-content:space-between"><form class="row"><input name="q" value="{{ request('q') }}" placeholder="Search title" style="padding:9px;border:1px solid #ccc;border-radius:7px"><select name="status" style="padding:9px;border:1px solid #ccc;border-radius:7px"><option value="">All statuses</option>@foreach(['draft','review','scheduled','published'] as $s)<option @selected(request('status')===$s)>{{ $s }}</option>@endforeach</select><button class="btn secondary">Filter</button></form><a class="btn" href="{{ route('admin.articles.create') }}">+ New article</a></div><table><tr><th>Article</th><th>Site</th><th>Category</th><th>Status</th><th>Publish</th><th></th></tr>@foreach($articles as $a)<tr><td><b>{{ $a->title }}</b><div class="muted small">/{{ $a->slug }}</div></td><td>{{ $a->site?->name }}</td><td>{{ $a->category?->name }}</td><td><span class="badge">{{ $a->status }}</span></td><td>{{ optional($a->published_at)->format('Y-m-d H:i') }}</td><td><a href="{{ route('admin.articles.edit',$a) }}">Edit</a></td></tr>@endforeach</table><div class="pagination">{{ $articles->links() }}</div></div>@endsection
@extends('admin.layout') @section('heading','Configure Ad Slot') @section('content')<form class="card" method="post" action="{{ route('admin.ads.update',$slot) }}">@csrf @method('PUT')<h2>{{ $slot->key }}</h2><div class="field"><label>Label</label><input name="label" value="{{ old('label',$slot->label) }}"></div><div class="field"><label>Type</label><select name="type">@foreach(['display','anchor','interstitial','rewarded'] as $t)<option @selected($slot->type===$t)>{{ $t }}</option>@endforeach</select></div><div class="field"><label>Ad unit path</label><input name="ad_unit_path" value="{{ old('ad_unit_path',$slot->ad_unit_path) }}" placeholder="/YOUR_NETWORK/Site/banner_mid"></div><div class="field"><label>Sizes <span class="muted">comma/space separated: 300x250, 336x280</span></label><input name="sizes_text" value="{{ old('sizes_text',implode(', ',$slot->sizes??[])) }}"></div><label><input type="checkbox" name="enabled" value="1" @checked(old('enabled',$slot->enabled))> Enabled</label><p><button class="btn">Save slot</button></p></form>@endsection
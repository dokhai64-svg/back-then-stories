@extends('admin.layout') @section('heading',$artist->exists?'Edit Artist':'New Artist') @section('content')<form class="card" method="post" enctype="multipart/form-data" action="{{ $artist->exists?route('admin.artists.update',$artist):route('admin.artists.store') }}">@csrf @if($artist->exists)@method('PUT')@endif<div class="field"><label>Name</label><input name="name" value="{{ old('name',$artist->name) }}" required></div><div class="field"><label>Slug</label><input name="slug" value="{{ old('slug',$artist->slug) }}"></div><div class="field"><label>Bio</label><textarea name="bio">{{ old('bio',$artist->bio) }}</textarea></div><div class="field"><label>Image</label><input type="file" name="image_file" accept="image/*"></div><button class="btn">Save</button></form>@if($artist->exists)<form method="post" action="{{ route('admin.artists.destroy',$artist) }}" onsubmit="return confirm('Delete artist?')">@csrf @method('DELETE')<button class="btn danger" style="margin-top:14px">Delete</button></form>@endif@endsection
@extends('admin.layout') @section('heading','Dashboard') @section('content')<div class="stats">@foreach($stats as $k=>$v)<div class="card stat"><span class="muted">{{ ucfirst($k) }}</span><b>{{ number_format($v) }}</b></div>@endforeach</div><div class="card"><div class="row" style="justify-content:space-between"><h2>Recent articles</h2><a class="btn" href="{{ route('admin.articles.create') }}">+ New article</a></div><table><tr><th>Title</th><th>Site</th><th>Status</th><th>Author</th><th></th></tr>@foreach($recent as $a)<tr><td><b>{{ $a->title }}</b><div class="muted small">{{ $a->updated_at->diffForHumans() }}</div></td><td>{{ $a->site?->name }}</td><td><span class="badge">{{ $a->status }}</span></td><td>{{ $a->author?->name }}</td><td><a href="{{ route('admin.articles.edit',$a) }}">Edit</a></td></tr>@endforeach</table></div>@endsection
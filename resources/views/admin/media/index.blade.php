@extends('admin.layout') @section('heading','Media') @section('content')<div class="card"><form class="row" method="post" enctype="multipart/form-data" action="{{ route('admin.media.store') }}">@csrf<input type="file" name="file" accept="image/*" required><input name="alt_text" placeholder="Alt text" style="padding:9px;border:1px solid #ccc;border-radius:7px;min-width:260px"><button class="btn">Upload image</button></form></div><div class="media-grid" style="margin-top:18px">@foreach($media as $m)<div class="card media-item"><img src="{{ asset('storage/'.$m->path) }}" alt="{{ $m->alt_text }}"><p class="small" style="word-break:break-all">{{ asset('storage/'.$m->path) }}</p><button class="btn secondary small" type="button" onclick="navigator.clipboard.writeText('{{ asset('storage/'.$m->path) }}')">Copy URL</button><form method="post" action="{{ route('admin.media.destroy',$m) }}" onsubmit="return confirm('Delete image?')">@csrf @method('DELETE')<button class="btn danger small" style="margin-top:6px">Delete</button></form></div>@endforeach</div><div class="pagination">{{ $media->links() }}</div>@endsection